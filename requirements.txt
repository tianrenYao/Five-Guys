# Backend
Flask==3.1.0
flask-cors==5.0.1
PyMySQL==1.1.1
werkzeug==3.1.3

# Production WSGI server (required for VM deployment behind Nginx)
gunicorn==23.0.0

# PDF report generation (HTML-to-PDF via WeasyPrint)
weasyprint==62.3
# Transitive deps pinned — weasyprint 62.3 breaks on newer API versions
pydyf==0.10.0          # 0.11+ removed .transform() -> AttributeError at write_pdf
tinycss2==1.3.0        # known-good with weasyprint 62
cssselect2==0.7.0      # known-good with weasyprint 62
Jinja2==3.1.4

# Bill OCR (optional: install tesseract-ocr system package separately)
pytesseract==0.3.13
Pillow==10.3.0
pdfplumber==0.11.0

# Email notifications
Flask-Mail==0.10.0

# Environment config
python-dotenv==1.1.0

# CA bundle for outbound HTTPS (DeepSeek API, etc.)
certifi>=2024.2.2

# Markdown -> HTML rendering for AI-generated ESG analysis
markdown==3.7
